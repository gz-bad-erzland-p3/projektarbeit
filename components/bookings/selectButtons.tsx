export default function SelectButtons(props: any) {
    return(
        <div>
            <button className="w-full bg-green-600">
                {props.title}
            </button>
        </div>
    )
}